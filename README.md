HTML Structure

<!DOCTYPE html> defines the document type.

<html>, <head>, and <body> are the main sections of the page.

Head Section

<meta charset="UTF-8"> → character encoding.

<meta name="viewport"...> → makes the page responsive.

<title> → gives the webpage a title (shown on the browser tab).

Heading & Text Formatting

<h1> → Big main heading "ARTIST", styled with color pinkish-red, centered, and text is italic, bold, and underlined.

<h2> → Subheading "Pablo Picasso".

<p> → Paragraph explaining who an artist is, including references to Rembrandt.

Inside the paragraph:

<i> → italic text.

<b> → bold text.

<u> → underline.

<br> → line breaks.

<hr> → horizontal line separator.

Hyperlinks

<a href="..."> → Clickable link to learn more about artists.

Another <a> tag is empty (no URL).

Images

Four images (download.jpg, 14.jpg, 15.jpg, 16.jpg) are inserted with fixed height=150 and width=150.

Lists

<ul> → Unordered list with the item Techniques.

Nested <ol> → Ordered list showing: mind, internet, laptop.

Table

<table> → Creates a table with border=10, cellpadding=8, cellspacing=8, and yellow background.

<tr> → Table rows.

<th> → Header cells ("Sr. No.", "Name").

<td> → Data cells (Radhe Radhe, paints).

🎯 Purpose of the Code

To introduce Pablo Picasso and explain about artists.

To demonstrate basic HTML tags like headings, paragraphs, line breaks, formatting, hyperlinks, images, lists, and tables.

Useful as a beginner-level HTML project or assignment
